# Environment Configuration
# Set to 'development', 'test', or 'production'
ENV=development

# Logging Configuration
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Ollama Configuration
# Model name for the Italian translation
OLLAMA_MODEL=llama-3.1-8b-italian

# Optional: Custom Ollama host (if not using default)
# OLLAMA_HOST=http://localhost:11434

# Optional: API Keys (if needed for other services)
# OPENAI_API_KEY=your_openai_api_key
